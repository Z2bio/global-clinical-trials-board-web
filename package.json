{
  "name": "global-clinical-trials-public-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GitHub Pages static website for querying ClinicalTrials.gov public data.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "audit": "node scripts/audit.mjs",
    "check": "npm run test && npm run audit",
    "serve": "python3 -m http.server 8080"
  },
  "license": "MIT"
}
